#ifndef STRUCTS_HPP
#define STRUCTS_HPP

#include "structs.hpp"

namespace str {
	const MenuItem* menu(const MenuItem* current);

	const MenuItem* exit(const MenuItem* current);

	const MenuItem* leti(const MenuItem* current);
	const MenuItem* spbsu(const MenuItem* current);
	const MenuItem* spbpu(const MenuItem* current);
	const MenuItem* itmo(const MenuItem* current);
	const MenuItem* back(const MenuItem* current);
}

#endif